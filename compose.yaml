services:
  postgres:
    image: 'postgres:18.4'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '9876:5432'
    volumes:
      - pgdata:/var/lib/postgresql/

volumes:
  pgdata:
